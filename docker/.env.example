# docker/.env.example
# Copy to docker/.env (already in .gitignore) to override docker-compose defaults.
# These are only for local dev — never used in production.

POSTGRES_USER=niyam
POSTGRES_PASSWORD=niyam
POSTGRES_DB=niyamdrishti
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
